import React from "react";

const Button = ({ text }) => {
  return (
    <div className="bg-[#F3E063] px-3 py-2 rounded-md text-black font-semibold">
      {text}
    </div>
  );
};

export default Button;
